@model SpendLess.Components.SpendLessComponents.SpendLessNavigationBarModel

<div class="is-flex is-flex-direction-column" style="gap:10px;">
    @foreach(var item in Model.Items)
    {
        <a 
            class="button" 
            href="/@item.Slug"
            hx-ext="multi-swap"
            hx-boost="true"
            hx-swap="multi:#content,#navigation"
        >@item.Title</a>
        
    }
</div>
